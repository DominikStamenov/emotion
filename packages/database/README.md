# @repo/database

Shared generated Supabase database types. The SQL source of truth lives in
`supabase/migrations`; regenerate `src/database.types.ts` from the linked
Supabase project after applying migrations.
